| Check | Priority | Location | Issue | Suggested Solution |
|---|---|---|---|---|
| Checked | P0 | src/file_tree.c:167/172/177 + ~12 sites across file_tree.c & tui.c | Sign-compare / sign-conversion at int↔size_t boundaries (count/capacity are size_t in the DA API; tree indices are int). Blocks release build under -Werror. | Migrate file-tree and TUI indices to size_t, use SIZE_MAX-based sentinels, and update comparisons consistently. |
| Checked | P1 | src/file_tree.c:94 | walk's descending loop `for (int i = tmp_tree.count - 1; …)` underflows to SIZE_MAX on an empty dir, then narrows to int. Currently harmless because the loop guard rejects it, but it's UB on the conversion. | Guard the empty case before calculating `count - 1`, then decrement a size_t index with an explicit zero check. |
|  | P1 | src/tui.c:54 + handle_key call sites | handle_key conflates "user pressed q" and "an error occurred" — both return 1, so run_tui's main loop can't tell them apart and a real error looks like a clean exit. (The existing `// TODO("Handle handle_key error")` flags this.) | Distinguish the two — e.g. 0 = continue, 1 = quit, 2 = error — and have run_tui's loop branch on the value (or use a small enum). |
| Checked | P2 | src/file_tree.c:7, src/file_tree.c:17 | entry_cmp and walk are extern but have no prototype (-Wmissing-prototypes). | Mark both static. |
|  | P2 | src/tui.c:285 | handle_key's switch on `ch` has no default arm (-Wswitch-default). | Add `default: break;`. |
